namespace Sylvester

open FSharp.Quotations

open Sylvester.Arithmetic

open SetTheory
open Vector


module Functions =
    let diff (f:real->real) (x:real) = formula<real> 
    
    let lim (f:real->real) (x:real) (c:real) = real
    
